# =============================================================================
# Base Requirements - Core packages for all models
# =============================================================================
# Install: pip install -r requirements.txt
#
# This provides: Identity, CART, DPCART, SMOTE, AIM (CPU models)
# For GPU models: pip install -r requirements-gpu.txt
# For CPU torch:  pip install -r requirements-cpu.txt
# For synthcity:  pip install -r requirements-synthcity.txt
# =============================================================================

# Data handling
pandas>=2.0.0
numpy>=1.26.4,<2.0.0

# Machine learning
scikit-learn>=1.7.2
scipy>=1.11.4
imbalanced-learn>=0.14.1

# Encoding
category_encoders>=2.6.0

# Utilities
tqdm>=4.60.0
tomli>=2.0.0
tomli_w>=1.2.0
icecream>=2.1.0
seaborn

# Testing
pytest>=7.0.0
