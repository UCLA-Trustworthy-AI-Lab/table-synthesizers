# Core dependencies - CRITICAL: Use exact versions for compatibility
torch==2.3.1                       # Pinned as requested by user
torchvision==0.18.1              # Compatible with PyTorch 2.3.1
pandas>=2.0.0
numpy<=2.0.0
scikit-learn==1.7.2              # Upgraded for TabSyn compatibility
scipy==1.11.4                    # Downgraded for TabSyn _lazywhere compatibility

# Essential dependencies for most synthesizers
category_encoders>=2.6.0
tomli>=2.0.0
tomli_w>=1.2.0                   # Required for TabSyn
imbalanced-learn==0.14.0         # Upgraded for sklearn 1.7.2 compatibility
icecream>=2.1.0                  # Required for TabSyn debugging output

# For TabDDPM and TabSyn synthesizers - COMMENTED OUT: Use zero_workaround.py instead
# zero_workaround.py provides 100% functional replacement with subprocess compatibility
#libzero==0.0.8

# Development and testing
pytest>=7.0.0
tqdm>=4.60.0

# LTM (Latent Table Model) dependencies
transformers>=4.0.0
datasets>=2.0.0
accelerate>=0.20.0
ema-pytorch>=0.7.0
boto3>=1.26.0                    # Required for LTM S3 storage backend

# Synthcity models (BayesianNetwork, GREAT, ARF, NFlow)
# INSTALLED MANUALLY IN DOCKERFILE TO BYPASS TORCH VERSION CHECK
# synthcity>=0.2.12                # External package for advanced synthesizers

seaborn

# Optional: For AIM synthesizer (requires complex private-pgm setup)
# Currently disabled due to dependency complexity
# opendp==0.7.0
# autodp==0.2
# private-pgm @ git+https://github.com/ryan112358/private-pgm.git

# Note for Apple Silicon (ARM64) users:
# Use conda to install core packages for better ARM64 compatibility:
# conda install pytorch numpy pandas scikit-learn -c pytorch
# Then use pip for the remaining dependencies

# Conda Environment Setup for LTM Testing:
# 1. Activate your conda environment: conda activate <your-env-name>
# 2. Install LTM dependencies: pip install transformers datasets accelerate ema-pytorch
# 3. Set Python path: export PYTHONPATH="$PWD/src/stg/LTM-VAE/src:$PYTHONPATH"
# 4. Run tests: pytest test_ltm_integration_basic.py -v
# Or use the convenience script: ./run_tests_conda.sh
